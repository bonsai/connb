{
  "name": "connb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "connb": "dist/index.js"
  },
  "scripts": {
    "build": "bun node_modules/typescript/bin/tsc -p tsconfig.json",
    "dev": "bun node_modules/typescript/bin/tsc -p tsconfig.json --watch",
    "start": "bun dist/index.js",
    "typecheck": "bun node_modules/typescript/bin/tsc -p tsconfig.json --noEmit",
    "lint": "bun node_modules/typescript/bin/tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  }
}
